import { VolunteerFilterPipePipe } from './volunteer-filter-pipe.pipe';

describe('VolunteerFilterPipePipe', () => {
  it('create an instance', () => {
    const pipe = new VolunteerFilterPipePipe();
    expect(pipe).toBeTruthy();
  });
});
